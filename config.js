/**
 * Centralized Game Configuration
 *
 * Administrators can edit the settings in this file to customize game behavior:
 * - gameTime: Maximum time allowed to complete the game (in seconds).
 * - backUrl: Destination URL for the "بازگشت" (Back) button.
 */
const CONFIG = {
    // Game countdown timer duration in seconds
    gameTime: 30,

    // Back button redirect URL
    backUrl: "https://example.com"
};
